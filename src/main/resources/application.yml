server:
  port: 8084

spring:
  application:
    name: ms-bff

logging:
  level:
    cl.pymetrack.msbff: DEBUG
    org.springframework.web.reactive: INFO

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

services:
  ms-user: http://172.31.45.64:8085/api/v1
  ms-productos: http://172.31.43.46:8081/api/v1
  ms-pedidos: http://172.31.2.110:8082/api/v1

app:
  bff:
    timeout: 5000
    retry-attempts: 2